# Uncomment the next line to define a global platform for your project
platform :ios, '12.1'

target 'graphql-client-demo' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!
  pod 'Apollo'
  pod 'SwiftLint', '0.28.0'
  # Pods for ApolloClient Test

  target 'graphql-client-demoTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'graphql-client-demoUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
